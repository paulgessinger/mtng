\providecolor{Purple}{rgb}{0.56,0.27,0.64}
\providecolor{LimeGreen}{rgb}{0.53,0.79,0.27}
\providecolor{Yellow}{rgb}{1,0.92,0.24}
\providecolor{beige}{HTML}{ffebc6}

\usepackage{iftex}

\ifluatex
\usepackage{emoji}
\providecommand{\cusemoji}[1]{\raisebox{-0.15\height}{\emoji{#1}}}
\providecommand{\emojispace}[1]{\raisebox{-0.15\height}{\emoji{#1} }}
\fi

\providecommand{\prstr}[1]{\##1}
\providecommand{\issstr}[1]{\##1}

\usepackage{tikz}
\usetikzlibrary{calc,arrows,arrows.meta,external}
\tikzset{>={Triangle[angle=50:0.13em 3]}}

\providecommand{\prmerged}{
  \normalsize
  \tikzsetnextfilename{prmerged}
  \hspace*{-0.8em}
  \raisebox{-0.3\height}{
  \begin{tikzpicture}[line width=1pt,color=Purple]
    \begin{scope}[transform canvas={scale=0.80}]
      \node[draw,circle,inner sep=0,minimum width=0.27em] (bl) at (0,0) {};
      \node[draw,circle,inner sep=0,minimum width=0.27em] (mr) at (0.9em,0.4em) {};
      \node[draw,circle,inner sep=0,minimum width=0.27em] (tl) at (0,1em) {};
      \draw[shorten <=-0.1pt,shorten >=-0.1pt] (mr) to[in=-90,out=180] (tl);
      \draw[shorten >=-0.1pt,shorten <=-0.1pt] (bl) -- (tl);
    \end{scope}
    \coordinate (tl) at (-0.3em,-0.3em);
    \coordinate (br) at (1.1em,1em);
    \clip (tl) rectangle (br);
  \end{tikzpicture}
  \hspace*{-0.9em}
  }
}


\providecommand{\propen}{
  \normalsize
  % \tikzset{external/export next=false}
  \tikzsetnextfilename{propen}
  \hspace*{-0.8em}
  \raisebox{-0.3\height}{
  \begin{tikzpicture}[line width=1pt,color=LimeGreen]
    \begin{scope}[transform canvas={scale=0.80}]
      \node[draw,circle,inner sep=0,minimum width=0.27em] (bl) at (0,0) {};
      \node[draw,circle,inner sep=0,minimum width=0.27em] (br) at (0.9em,0) {};
      \node[draw,circle,inner sep=0,minimum width=0.27em] (tl) at (0,1em) {};
      \draw[->,shorten <=-0.1pt,shorten >=-0.1pt] (br) --++(0,0.5em) ..controls ++(0,0.4em) and ++(0.5em,0).. (tl);
      \draw[shorten >=-0.1pt,shorten <=-0.1pt] (bl) -- (tl);
    \end{scope}
    \coordinate (tl) at (-0.3em,-0.3em);
    \coordinate (br) at (1.1em,1em);
    \clip (tl) rectangle (br);
  \end{tikzpicture}
  \hspace*{-0.9em}
}
}

\providecommand{\prwip}{
  \normalsize
  \tikzsetnextfilename{prwip}
  \hspace*{-1.1em}
  \raisebox{-0.3\height}{
  \begin{tikzpicture}[]
    \node[rectangle,fill=Yellow,rounded corners=0.5em,
          minimum height=1.55em,minimum width=2.9em] (t) {\small\emojispace{construction}WIP};
  \end{tikzpicture}
  \hspace*{-0.8em}
}
}

\providecommand{\prstale}{
  \normalsize
  \tikzsetnextfilename{prstale}
  \hspace*{-1.1em}
  \raisebox{-0.3\height}{
  \begin{tikzpicture}[]
    \node[rectangle,fill=beige,rounded corners=0.5em,
          minimum height=1.55em] (t) {\small{}Stale};
  \end{tikzpicture}
  \hspace*{-0.8em}
}
}

%% ---- bento statistics tiles ----------------------------------------------
\providecolor{bentotile}{HTML}{F2F2F7}
\providecolor{bentohero}{HTML}{E7E2F3}
\providecolor{bentoadd}{HTML}{DDF3E4}
\providecolor{bentodel}{HTML}{FBE3E4}
\providecolor{bentoink}{HTML}{1C1C1E}
\providecolor{bentomuted}{HTML}{6E6E73}

% Grid geometry, overridable by the host deck.
\providecommand{\bentocolw}{0.225\textwidth}
\providecommand{\bentorowh}{1.7cm}
\providecommand{\bentogutter}{0.18cm}
\providecommand{\bentoradius}{0.22cm}

\tikzset{bento tile/.style={
    rectangle, draw=none, rounded corners=\bentoradius,
    anchor=north west, align=left,
    inner xsep=0.7em, inner ysep=0.6em, outer sep=0pt}}

% Solve the grid geometry for one tile: #1 col #2 row #3 colspan #4 rowspan.
% pgfmath is required because \bentocolw expands to a factor times a dimension,
% which cannot be multiplied again directly. Row 0 is at the top, hence the
% negative y and the north west anchor.
\providecommand{\bentogeom}[4]{%
  \pgfmathsetlengthmacro{\bentotmpw}{#3*(\bentocolw)+(#3-1)*(\bentogutter)}%
  \pgfmathsetlengthmacro{\bentotmph}{#4*(\bentorowh)+(#4-1)*(\bentogutter)}%
  \pgfmathsetlengthmacro{\bentotmpx}{#1*((\bentocolw)+(\bentogutter))}%
  \pgfmathsetlengthmacro{\bentotmpy}{-#2*((\bentorowh)+(\bentogutter))}%
}

% #1 col #2 row #3 colspan #4 rowspan #5 fill #6 value #7 caption
\providecommand{\bentotile}[7]{%
  \bentogeom{#1}{#2}{#3}{#4}%
  \node[bento tile, fill=#5,
        minimum width=\bentotmpw, minimum height=\bentotmph,
        text width=\dimexpr\bentotmpw-1.6em\relax]
    at (\bentotmpx,\bentotmpy)
    {\raggedright
     % Each \par sits inside its own font group: the baselineskip in force at
     % the end of a paragraph sets the spacing of all its lines, so closing the
     % group first would space these with the node's default size instead.
     {\color{bentoink}\sffamily\bfseries\fontsize{20}{22}\selectfont #6\par}%
     \vspace{0.35em}%
     {\color{bentomuted}\sffamily\scriptsize #7\par}};%
}

% Same grid, but for a tile whose body is a short list rather than one numeral.
\providecommand{\bentolisttile}[7]{%
  \bentogeom{#1}{#2}{#3}{#4}%
  \node[bento tile, fill=#5,
        minimum width=\bentotmpw, minimum height=\bentotmph,
        text width=\dimexpr\bentotmpw-1.6em\relax]
    at (\bentotmpx,\bentotmpy)
    {\raggedright
     % See \bentotile: \par must close inside the font group. The list body is
     % multi-line, so this is where wrong baselineskip is most visible.
     {\color{bentoink}\sffamily\small\linespread{1.15}\selectfont #6\par}%
     \vspace{0.35em}%
     {\color{bentomuted}\sffamily\scriptsize #7\par}};%
}

\providecommand{\bentobox}[1]{%
  \begin{center}%
  \begin{tikzpicture}#1\end{tikzpicture}%
  \end{center}%
}

\providecommand{\iss}{
  % \tikzset{external/export next=false}
  \tikzsetnextfilename{iss}
  \normalsize
  \hspace*{-0.7em}
  \raisebox{-0.3\height}{
  \begin{tikzpicture}[line width=1pt,color=LimeGreen]
    \begin{scope}[transform canvas={scale=0.85},shift={(0.4em,0.5em)}]
      \draw[line width=1.0pt] (0,0) circle(0.6em);
      \fill[] (0,0) circle(0.15em);
    \end{scope}
    \coordinate (tl) at (-0.3em,-0.3em);
    \coordinate (br) at (1.1em,1em);
    \clip (tl) rectangle (br);
  \end{tikzpicture}
  \hspace*{-0.9em}
}
}