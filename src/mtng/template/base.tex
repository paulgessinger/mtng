\documentclass[aspectratio=169,t,13pt,dvipsnames]{beamer}

\setbeamertemplate{navigation symbols}{}
\setbeamersize{text margin left=8mm,text margin right=8mm}

{{ include_raw("preamble.tex") }}


\begin{document}

{% if deck_title %}
\title{ {{ deck_title|sanitize }} }
{% else %}
\title{Pull request and issue summary for \\{%- for repo_name, repo in repos.items() -%}{{ repo_name }}{% if not loop.last %}\\{% endif %}{%- endfor -%}}
{% endif %}
{% if footline_left %}
\author{ {{ footline_left|sanitize }} }
{% else %}
\author{mtng}
{% endif %}
\date{ {{ now.strftime('%Y-%m-%d') }} }
\frame[plain]{\titlepage}
  
{% block body %}
{% endblock %}


\end{document}